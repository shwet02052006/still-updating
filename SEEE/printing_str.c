#include <stdio.h>
int main()
{
//char st[] = {'a', 'b', 'c', '\0'}; // Strings are character array terminated by NULL character(\0)
char st[] = "abc"; //same as above 
printf("%s",st);








    // for (int i = 0; i < 3; i++)
    // {

    //     printf("%c", st[i]);
    // }
    // printf("First charater is : %c\n", st[0]);  
    //printf("%d%d",sizeof(st),sizeof(st[0]));
    return 0;
}